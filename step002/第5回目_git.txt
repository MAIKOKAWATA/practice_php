@5回目 2018/12/10
【復習】

リモートリポジトリを作成できるようにサーバーを用意するサービス=GitHub

環境変数はどっからでも呼べるもの。
Pathはプログラムが入っている場所を登録する。
Pathの中からプログラムの場所を探るようになっているので、
設定していないとcmdでphp -vとかたたいても認識できない。

1個のファイルでconfigを作った場合、
環境変数で開発の場合、本番の場合でそれぞれ設定しておくと、
1つのソースコードで開発環境も本番環境も動くようにできる。

ssh　サーバーに接続するコマンド
scp　ファイルアップのコマンド

----------------------------------------------------------------

phpunit ファイル名
でテストコード動く

----------------------------------------------------------------

echo "# favorite_YouTubers" >> README.md
README.txtに# favorite_YouTubersと書き込めの意味


このファイルcommitしますよーって言う準備　git add

git reset ファイル名
でインデックス取り下げ(git add)

ローカルファイルにリモートディレクトリの場所を教える

git push -u origin master
           github上　ブランチ名

----------------------------------------------------------------
リモート設定ミスったら
git set url


----------------------------------------------------------------
次回以降
cd ~でホームディレクトリに移動する

cd ~/.ssh
-----------------------------------------------------------------
